{
  "name": "rup-ai-phase-1-1",
  "version": "1.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate:dev": "prisma migrate dev",
    "prisma:migrate:deploy": "prisma migrate deploy",
    "prisma:migrate:reset": "prisma migrate reset",
    "prisma:studio": "prisma studio",
    "prisma:format": "prisma format",
    "prisma:validate": "prisma validate",
    "db:push": "prisma db push",
    "db:seed": "tsx prisma/seed/index.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:schema": "vitest run prisma/__tests__/schema",
    "test:seed": "vitest run prisma/__tests__/seed",
    "test:performance": "vitest run prisma/__tests__/performance",
    "test:M01": "vitest run --dir prisma",
    "test:M02": "vitest run --dir src/shared",
    "test:M03A": "vitest run --dir src/modules/template",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@prisma/client": "^5.7.0",
    "zod": "^3.22.4"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.0.8",
    "dotenv": "^17.2.3",
    "prisma": "^5.7.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^4.0.8"
  }
}